<!doctype html>
<html lang="es">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>AmorProof | La prueba diaria de amor propio</title>
  <style>
    :root{
      --bg:#0b1020;
      --card:#111a33;
      --text:#e9ecf5;
      --muted:#b9c1da;
      --accent:#a698d8;
      --accent2:#5db3a3;
      --radius:18px;
      --max:980px;
    }
    *{box-sizing:border-box}
    body{
      margin:0;
      font-family: ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Arial;
      background: radial-gradient(1200px 600px at 20% 10%, rgba(166,152,216,.25), transparent 60%),
                  radial-gradient(900px 500px at 80% 20%, rgba(93,179,163,.18), transparent 55%),
                  var(--bg);
      color:var(--text);
      line-height:1.5;
    }
    .wrap{max-width:var(--max); margin:0 auto; padding:22px;}
    .nav{
      position:sticky; top:0; backdrop-filter: blur(10px);
      background: rgba(11,16,32,.65);
      border-bottom: 1px solid rgba(255,255,255,.07);
      z-index:10;
    }
    .navin{display:flex; align-items:center; justify-content:space-between; gap:12px; padding:14px 22px; max-width:var(--max); margin:0 auto;}
    .brand{display:flex; align-items:center; gap:10px; font-weight:700}
    .dot{width:10px; height:10px; border-radius:999px; background: linear-gradient(90deg,var(--accent),var(--accent2));}
    .btns{display:flex; gap:10px; flex-wrap:wrap; justify-content:flex-end}
    .btn{
      border:1px solid rgba(255,255,255,.14);
      background: rgba(255,255,255,.06);
      color:var(--text);
      padding:10px 14px;
      border-radius:999px;
      cursor:pointer;
      font-weight:600;
    }
    .btn.primary{
      background: linear-gradient(90deg, rgba(166,152,216,.95), rgba(93,179,163,.85));
      border:0;
      color:#0b1020;
    }
    header{padding:42px 0 18px}
    .hero{
      display:grid;
      grid-template-columns: 1.2fr .8fr;
      gap:18px;
      align-items:stretch;
    }
    @media (max-width: 860px){ .hero{grid-template-columns:1fr;} }
    .card{
      background: rgba(17,26,51,.75);
      border: 1px solid rgba(255,255,255,.08);
      border-radius: var(--radius);
      box-shadow: 0 12px 40px rgba(0,0,0,.35);
      padding:18px;
    }
    .kicker{color:var(--muted); font-weight:600}
    h1{margin:8px 0 10px; font-size: clamp(28px, 3.4vw, 44px); line-height:1.1}
    .sub{color:var(--muted); font-size: 16px; max-width: 60ch}
    .pillrow{display:flex; gap:10px; flex-wrap:wrap; margin-top:14px}
    .pill{
      border:1px solid rgba(255,255,255,.12);
      padding:8px 10px;
      border-radius:999px;
      color:var(--muted);
      font-weight:600;
      font-size: 13px;
      background: rgba(255,255,255,.04);
    }
    section{padding:26px 0}
    .sectionTitle{display:flex; align-items:end; justify-content:space-between; gap:12px; margin-bottom:12px}
    h2{margin:0; font-size:22px}
    .hint{color:var(--muted); font-size:13px}
    .grid{
      display:grid;
      grid-template-columns: repeat(3, 1fr);
      gap:12px;
      margin-top:12px;
    }
    @media (max-width: 860px){ .grid{grid-template-columns:1fr;} }
    .metric{
      padding:14px;
      border-radius:16px;
      border:1px solid rgba(255,255,255,.08);
      background: rgba(255,255,255,.04);
    }
    .metric b{display:block; font-size:16px}
    .metric span{color:var(--muted); font-size:13px}
    .testis{display:grid; grid-template-columns: repeat(3, 1fr); gap:12px}
    @media (max-width: 860px){ .testis{grid-template-columns:1fr;} }
    input, textarea{
      width:100%;
      padding:12px 12px;
      border-radius:14px;
      border:1px solid rgba(255,255,255,.12);
      background: rgba(255,255,255,.04);
      color: var(--text);
      outline:none;
    }
    textarea{min-height: 110px; resize: vertical;}
    label{display:block; font-weight:700; margin:10px 0 6px}
    .formrow{display:grid; grid-template-columns: 1fr 1fr; gap:10px}
    @media (max-width: 860px){ .formrow{grid-template-columns:1fr;} }
    footer{padding:26px 0 40px; color:var(--muted); font-size:13px}
  </style>
</head>

<body>
  <div class="nav">
    <div class="navin">
      <div class="brand">
        <span class="dot"></span>
        <span>AmorProof</span>
      </div>
      <div class="btns">
        <button class="btn" onclick="go('#dashboard')">Dashboard</button>
        <button class="btn" onclick="go('#testimonios')">Testimonios</button>
        <button class="btn primary" onclick="go('#formulario')">Formulario</button>
      </div>
    </div>
  </div>

  <div class="wrap">
    <header>
      <div class="hero">
        <div class="card">
          <div class="kicker">La prueba diaria de amor propio</div>
          <h1>Calma, claridad y decisión.<br/>Una vez al día. 🌿</h1>
          <p class="sub">
            AmorProof es una app (en camino) para entrenar regulación emocional y decisiones conscientes,
            especialmente al despertar y en momentos de crisis.
          </p>
          <div class="pillrow">
            <div class="pill">Hábitos</div>
            <div class="pill">Regulación emocional</div>
            <div class="pill">Metas sostenibles</div>
            <div class="pill">Ritual de 3–7 min</div>
          </div>
          <div style="margin-top:14px; display:flex; gap:10px; flex-wrap:wrap;">
            <button class="btn primary" onclick="go('#formulario')">Quiero unirme</button>
            <button class="btn" onclick="go('#testimonios')">Ver testimonios</button>
          </div>
        </div>

        <div class="card" id="dashboard">
          <div class="kicker">Dashboard general</div>
          <div class="grid">
            <div class="metric">
              <b>¿Qué es?</b>
              <span>Entrenamiento diario, breve y medible.</span>
            </div>
            <div class="metric">
              <b>¿Qué cambia?</b>
              <span>Ansiedad a claridad; reacción a decisión.</span>
            </div>
            <div class="metric">
              <b>¿Cómo se mide?</b>
              <span>Streaks, check-ins, auto-reporte y progreso.</span>
            </div>
          </div>
          <div style="margin-top:12px" class="metric">
            <b>Ruta (simple)</b>
            <span>Identificar estado → regular → decidir → micro-acción → registro.</span>
          </div>
        </div>
      </div>
    </header>

    <section id="testimonios">
      <div class="sectionTitle">
        <h2>Testimonios</h2>
        <div class="hint">Cámbialos por los reales cuando los tengas.</div>
      </div>
      <div class="testis">
        <div class="card">“Me ayudó a dejar de pelear conmigo en la mañana.”<div style="margin-top:10px; color:var(--muted); font-weight:700;">Persona 1</div></div>
        <div class="card">“Es un ritual que me aterriza cuando me voy a la ansiedad.”<div style="margin-top:10px; color:var(--muted); font-weight:700;">Persona 2</div></div>
        <div class="card">“Ver progreso me mantiene constante.”<div style="margin-top:10px; color:var(--muted); font-weight:700;">Persona 3</div></div>
      </div>
    </section>

    <section id="formulario">
      <div class="sectionTitle">
        <h2>Formulario</h2>
        <div class="hint">Este formulario sí se guarda en Netlify y puede enviarte correo.</div>
      </div>

      <div class="card">
        <form name="amorproof-leads"
              method="POST"
              data-netlify="true"
              data-netlify-honeypot="bot-field"
              action="/thanks.html">

          <input type="hidden" name="form-name" value="amorproof-leads" />

          <p style="display:none;">
            <label>No llenar: <input name="bot-field" /></label>
          </p>

          <div class="formrow">
            <div>
              <label>Nombre</label>
              <input name="nombre" placeholder="Tu nombre" required />
            </div>
            <div>
              <label>Email</label>
              <input name="email" type="email" placeholder="tucorreo@ejemplo.com" required />
            </div>
          </div>

          <label>¿Qué te gustaría lograr con AmorProof?</label>
          <textarea name="meta" placeholder="Ej: reducir ansiedad al despertar..." required></textarea>

          <div style="margin-top:12px; display:flex; gap:10px; flex-wrap:wrap;">
            <button class="btn primary" type="submit">Enviar</button>
          </div>
        </form>
      </div>
    </section>

    <footer>
      © <span id="year"></span> AmorProof
    </footer>
  </div>

  <script>
    function go(hash){
      const el = document.querySelector(hash);
      if(!el) return;
      el.scrollIntoView({ behavior: "smooth", block: "start" });
    }
    document.getElementById("year").textContent = new Date().getFullYear();
  </script>
</body>
</html>
